{
	"name": "obsidian-sample-plugin",
	"version": "1.0.0",
	"description": "This is a sample plugin for Obsidian (https://obsidian.md)",
	"main": "main.js",
	"scripts": {
		"dev": "bun build main.ts --outdir=dist --outfile=main.js --external:obsidian --external:electron --external:@codemirror/* --external:@lezer/* --format=cjs --target=es2018 --sourcemap=inline",
		"build": "bun build main.ts --outdir=dist --outfile=main.js --external:obsidian --external:electron --external:@codemirror/* --external:@lezer/* --format=cjs --target=es2018 --minify",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"keywords": [],
	"author": "",
	"license": "MIT",
	"devDependencies": {
		"@types/bun": "^1.2.15",
		"builtin-modules": "^5.0.0",
		"obsidian": "^1.8.7",
		"tslib": "2.8.1"
	},
	"private": true,
	"dependencies": {
		"bun-types": "^1.2.15"
	}
}
